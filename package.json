{
  "name": "sign",
  "version": "0.0.1",
  "description": "Sign widget",
  "repository": {
    "type": "git",
    "url": "https://github.com/AlexKryvets/sign.git"
  },
  "author": "Alexey Kryvets",
  "scripts": {
    "build:dev": "npm run clear:dist && webpack --progress --profile",
    "build:prod": "npm run clear:dist && webpack --progress --profile --bail",
    "clear:dist": "rimraf -- dist",
    "watch:dev": "npm run build:dev -- --watch",
    "watch:prod": "npm run build:prod -- --watch"
  },
  "devDependencies": {
    "@types/node": "^8.0.24",
    "rimraf": "^2.6.1",
    "typescript": "^2.4.2",
    "browser-sync": "^2.18.13",

    "webpack": "^3.5.2",
    "ts-loader": "^2.3.2",
    "browser-sync-webpack-plugin": "^1.2.0"
  }
}
